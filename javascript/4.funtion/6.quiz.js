import { gugudan, selectGugudan, fruitsTower } from "./fmodules.js";

// 구구단을 함수로 호출
gugudan(); // 1~9단 출력
selectGugudan(3, 5); // 3~5단 출력
fruitsTower(5, `🍎`);
fruitsTower(7, `🍊`);
fruitsTower(10, `🍑`);
